schema: spec-driven

context: |
  Tech stack: React 18 + TypeScript + Vite 6 + Tailwind 4
  Architecture: shadcn/ui components, MUI, Supabase backend, React Router
  Testing: No test framework detected
  Style: tsconfig strict mode, no linter/formatter configured

strict_tdd: false

testing:
  ## Testing Capabilities
  **Strict TDD Mode**: disabled
  **Detected**: 2026-05-05

  ### Test Runner
  - Command: `NOT AVAILABLE`
  - Framework: None detected

  ### Test Layers
  | Layer | Available | Tool |
  |-------|-----------|------|
  | Unit | ❌ | — |
  | Integration | ❌ | — |
  | E2E | ❌ | — |

  ### Coverage
  - Available: ❌
  - Command: `—`

  ### Quality Tools
  | Tool | Available | Command |
  |------|-----------|---------|
  | Linter | ❌ | — |
  | Type checker | ✅ | `tsc --noEmit` |
  | Formatter | ❌ | — |

  **Note**: Strict TDD Mode is disabled — no test runner detected in the project.
  To enable TDD, add a test framework (Vitest recommended for Vite projects).

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbers (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)
